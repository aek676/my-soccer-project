import { NodePlayerProvider } from './node-player-provider';

describe('NodePlayerProvider', () => {
  it('should create an instance', () => {
    expect(new NodePlayerProvider()).toBeTruthy();
  });
});
